{
    "name": "root",
    "private": true,
    "devDependencies": {
        "@trivago/prettier-plugin-sort-imports": "^4.3.0",
        "lerna": "^3.22.1",
        "prettier": "^3.2.5"
    },
    "scripts": {
        "format": "prettier --write packages/",
        "watch": "lerna run watch --stream --no-sort --concurrency 99",
        "i18n": "lerna run i18n --scope=@atlasacademy/db --stream",
        "start:db": "lerna run start --scope=@atlasacademy/db --stream",
        "start:paper-moon": "lerna run start --scope=paper-moon --stream"
    }
}
